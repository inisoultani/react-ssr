import React from 'react';

const Home = () => {
  return (
    <div>
      <div>this is home components</div>
      <button onClick={() => console.log("button home clicked")}>Click me!</button>
    </div>
  );
};

export default Home;
